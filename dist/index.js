import { SvelteComponentDev, init, safe_not_equal, dispatch_dev, validate_slots, empty, insert_dev, group_outros, transition_out, check_outros, transition_in, detach_dev, handle_promise, update_await_block_branch, element, add_location, action_destroyer, listen_dev, noop, run_all, create_slot, update_slot_base, get_all_dirty_from_scope, get_slot_changes, text } from 'svelte/internal';

// generated during release, do not modify

const PUBLIC_VERSION = '4';

if (typeof window !== 'undefined')
	// @ts-ignore
	(window.__svelte || (window.__svelte = { v: new Set() })).v.add(PUBLIC_VERSION);

/**
 * Creates an Intersection Observer for monitoring element visibility within the viewport.
 *
 * When the element enters or exits the viewport, a corresponding custom event
 * (`enterViewport` or `exitViewport`) is dispatched on the element.
 *
 * @param {Element} element - The DOM element to observe for visibility changes.
 * @returns {Object} An object containing a `destroy` method to stop observing the element.
 *
 * @example
 * const observer = viewport(document.querySelector('#myElement'));
 * // To stop observing later:
 * observer.destroy();
 */
const viewport = (element) => {
    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
            const eventName = entry.isIntersecting
                ? "enterViewport"
                : "exitViewport";
            entry.target.dispatchEvent(new CustomEvent(eventName));
        });
    });
    observer.observe(element);
    return {
        destroy() {
            observer.unobserve(element);
            // Optionally disconnect the observer if no longer needed.
            if (observer.takeRecords().length === 0) {
                observer.disconnect();
            }
        },
    };
};

/* src\components\LazyComponent.svelte generated by Svelte v4.2.19 */
const file = "src\\components\\LazyComponent.svelte";

function get_then_context(ctx) {
	ctx[6] = ctx[7].default;
}

const get_component_slot_changes = dirty => ({
	Component: dirty & /*componentPromise*/ 2
});

const get_component_slot_context = ctx => ({ Component: /*Component*/ ctx[6] });
const get_fallback_slot_changes = dirty => ({});
const get_fallback_slot_context = ctx => ({});

// (14:0) {:else}
function create_else_block(ctx) {
	let await_block_anchor;
	let promise;
	let current;

	let info = {
		ctx,
		current: null,
		token: null,
		hasCatch: false,
		pending: create_pending_block,
		then: create_then_block,
		catch: create_catch_block,
		value: 7,
		blocks: [,,,]
	};

	handle_promise(promise = /*componentPromise*/ ctx[1], info);

	const block = {
		c: function create() {
			await_block_anchor = empty();
			info.block.c();
		},
		m: function mount(target, anchor) {
			insert_dev(target, await_block_anchor, anchor);
			info.block.m(target, info.anchor = anchor);
			info.mount = () => await_block_anchor.parentNode;
			info.anchor = await_block_anchor;
			current = true;
		},
		p: function update(new_ctx, dirty) {
			ctx = new_ctx;
			info.ctx = ctx;

			if (dirty & /*componentPromise*/ 2 && promise !== (promise = /*componentPromise*/ ctx[1]) && handle_promise(promise, info)) ; else {
				update_await_block_branch(info, ctx, dirty);
			}
		},
		i: function intro(local) {
			if (current) return;
			transition_in(info.block);
			current = true;
		},
		o: function outro(local) {
			for (let i = 0; i < 3; i += 1) {
				const block = info.blocks[i];
				transition_out(block);
			}

			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(await_block_anchor);
			}

			info.block.d(detaching);
			info.token = null;
			info = null;
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_else_block.name,
		type: "else",
		source: "(14:0) {:else}",
		ctx
	});

	return block;
}

// (12:0) {#if !isShowingComponent}
function create_if_block(ctx) {
	let div;
	let mounted;
	let dispose;

	const block = {
		c: function create() {
			div = element("div");
			add_location(div, file, 19, 2, 324);
		},
		m: function mount(target, anchor) {
			insert_dev(target, div, anchor);

			if (!mounted) {
				dispose = [
					action_destroyer(viewport.call(null, div)),
					listen_dev(div, "enterViewport", /*handleEnterViewport*/ ctx[2], false, false, false, false)
				];

				mounted = true;
			}
		},
		p: noop,
		i: noop,
		o: noop,
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(div);
			}

			mounted = false;
			run_all(dispose);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_if_block.name,
		type: "if",
		source: "(12:0) {#if !isShowingComponent}",
		ctx
	});

	return block;
}

// (1:0) <script lang="ts">import viewport from "../lib/viewport-action"; let loadComponent; export { loadComponent as this }
function create_catch_block(ctx) {
	const block = {
		c: noop,
		m: noop,
		p: noop,
		i: noop,
		o: noop,
		d: noop
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_catch_block.name,
		type: "catch",
		source: "(1:0) <script lang=\\\"ts\\\">import viewport from \\\"../lib/viewport-action\\\"; let loadComponent; export { loadComponent as this }",
		ctx
	});

	return block;
}

// (17:2) {:then { default: Component }}
function create_then_block(ctx) {
	get_then_context(ctx);
	let current;
	const component_slot_template = /*#slots*/ ctx[5].component;
	const component_slot = create_slot(component_slot_template, ctx, /*$$scope*/ ctx[4], get_component_slot_context);

	const block = {
		c: function create() {
			if (component_slot) component_slot.c();
		},
		m: function mount(target, anchor) {
			if (component_slot) {
				component_slot.m(target, anchor);
			}

			current = true;
		},
		p: function update(ctx, dirty) {
			get_then_context(ctx);

			if (component_slot) {
				if (component_slot.p && (!current || dirty & /*$$scope, componentPromise*/ 18)) {
					update_slot_base(
						component_slot,
						component_slot_template,
						ctx,
						/*$$scope*/ ctx[4],
						!current
						? get_all_dirty_from_scope(/*$$scope*/ ctx[4])
						: get_slot_changes(component_slot_template, /*$$scope*/ ctx[4], dirty, get_component_slot_changes),
						get_component_slot_context
					);
				}
			}
		},
		i: function intro(local) {
			if (current) return;
			transition_in(component_slot, local);
			current = true;
		},
		o: function outro(local) {
			transition_out(component_slot, local);
			current = false;
		},
		d: function destroy(detaching) {
			if (component_slot) component_slot.d(detaching);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_then_block.name,
		type: "then",
		source: "(17:2) {:then { default: Component }}",
		ctx
	});

	return block;
}

// (15:27)       <slot name="fallback">Loading...</slot>    {:then { default: Component }}
function create_pending_block(ctx) {
	let current;
	const fallback_slot_template = /*#slots*/ ctx[5].fallback;
	const fallback_slot = create_slot(fallback_slot_template, ctx, /*$$scope*/ ctx[4], get_fallback_slot_context);
	const fallback_slot_or_fallback = fallback_slot || fallback_block(ctx);

	const block = {
		c: function create() {
			if (fallback_slot_or_fallback) fallback_slot_or_fallback.c();
		},
		m: function mount(target, anchor) {
			if (fallback_slot_or_fallback) {
				fallback_slot_or_fallback.m(target, anchor);
			}

			current = true;
		},
		p: function update(ctx, dirty) {
			if (fallback_slot) {
				if (fallback_slot.p && (!current || dirty & /*$$scope*/ 16)) {
					update_slot_base(
						fallback_slot,
						fallback_slot_template,
						ctx,
						/*$$scope*/ ctx[4],
						!current
						? get_all_dirty_from_scope(/*$$scope*/ ctx[4])
						: get_slot_changes(fallback_slot_template, /*$$scope*/ ctx[4], dirty, get_fallback_slot_changes),
						get_fallback_slot_context
					);
				}
			}
		},
		i: function intro(local) {
			if (current) return;
			transition_in(fallback_slot_or_fallback, local);
			current = true;
		},
		o: function outro(local) {
			transition_out(fallback_slot_or_fallback, local);
			current = false;
		},
		d: function destroy(detaching) {
			if (fallback_slot_or_fallback) fallback_slot_or_fallback.d(detaching);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_pending_block.name,
		type: "pending",
		source: "(15:27)       <slot name=\\\"fallback\\\">Loading...</slot>    {:then { default: Component }}",
		ctx
	});

	return block;
}

// (16:26) Loading...
function fallback_block(ctx) {
	let t;

	const block = {
		c: function create() {
			t = text("Loading...");
		},
		m: function mount(target, anchor) {
			insert_dev(target, t, anchor);
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(t);
			}
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: fallback_block.name,
		type: "fallback",
		source: "(16:26) Loading...",
		ctx
	});

	return block;
}

function create_fragment(ctx) {
	let current_block_type_index;
	let if_block;
	let if_block_anchor;
	let current;
	const if_block_creators = [create_if_block, create_else_block];
	const if_blocks = [];

	function select_block_type(ctx, dirty) {
		if (!/*isShowingComponent*/ ctx[0]) return 0;
		return 1;
	}

	current_block_type_index = select_block_type(ctx);
	if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);

	const block = {
		c: function create() {
			if_block.c();
			if_block_anchor = empty();
		},
		l: function claim(nodes) {
			throw new Error("options.hydrate only works if the component was compiled with the `hydratable: true` option");
		},
		m: function mount(target, anchor) {
			if_blocks[current_block_type_index].m(target, anchor);
			insert_dev(target, if_block_anchor, anchor);
			current = true;
		},
		p: function update(ctx, [dirty]) {
			let previous_block_index = current_block_type_index;
			current_block_type_index = select_block_type(ctx);

			if (current_block_type_index === previous_block_index) {
				if_blocks[current_block_type_index].p(ctx, dirty);
			} else {
				group_outros();

				transition_out(if_blocks[previous_block_index], 1, 1, () => {
					if_blocks[previous_block_index] = null;
				});

				check_outros();
				if_block = if_blocks[current_block_type_index];

				if (!if_block) {
					if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
					if_block.c();
				} else {
					if_block.p(ctx, dirty);
				}

				transition_in(if_block, 1);
				if_block.m(if_block_anchor.parentNode, if_block_anchor);
			}
		},
		i: function intro(local) {
			if (current) return;
			transition_in(if_block);
			current = true;
		},
		o: function outro(local) {
			transition_out(if_block);
			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) {
				detach_dev(if_block_anchor);
			}

			if_blocks[current_block_type_index].d(detaching);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_fragment.name,
		type: "component",
		source: "",
		ctx
	});

	return block;
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	validate_slots('LazyComponent', slots, ['fallback','component']);
	let { this: loadComponent } = $$props;
	let isShowingComponent = false;
	let componentPromise;

	const handleEnterViewport = () => {
		$$invalidate(1, componentPromise = loadComponent());
		$$invalidate(0, isShowingComponent = true);
	};

	$$self.$$.on_mount.push(function () {
		if (loadComponent === undefined && !('this' in $$props || $$self.$$.bound[$$self.$$.props['this']])) {
			console.warn("<LazyComponent> was created without expected prop 'this'");
		}
	});

	const writable_props = ['this'];

	Object.keys($$props).forEach(key => {
		if (!~writable_props.indexOf(key) && key.slice(0, 2) !== '$$' && key !== 'slot') console.warn(`<LazyComponent> was created with unknown prop '${key}'`);
	});

	$$self.$$set = $$props => {
		if ('this' in $$props) $$invalidate(3, loadComponent = $$props.this);
		if ('$$scope' in $$props) $$invalidate(4, $$scope = $$props.$$scope);
	};

	$$self.$capture_state = () => ({
		viewport,
		loadComponent,
		isShowingComponent,
		componentPromise,
		handleEnterViewport
	});

	$$self.$inject_state = $$props => {
		if ('loadComponent' in $$props) $$invalidate(3, loadComponent = $$props.loadComponent);
		if ('isShowingComponent' in $$props) $$invalidate(0, isShowingComponent = $$props.isShowingComponent);
		if ('componentPromise' in $$props) $$invalidate(1, componentPromise = $$props.componentPromise);
	};

	if ($$props && "$$inject" in $$props) {
		$$self.$inject_state($$props.$$inject);
	}

	return [
		isShowingComponent,
		componentPromise,
		handleEnterViewport,
		loadComponent,
		$$scope,
		slots
	];
}

class LazyComponent extends SvelteComponentDev {
	constructor(options) {
		super(options);
		init(this, options, instance, create_fragment, safe_not_equal, { this: 3 });

		dispatch_dev("SvelteRegisterComponent", {
			component: this,
			tagName: "LazyComponent",
			options,
			id: create_fragment.name
		});
	}

	get this() {
		throw new Error("<LazyComponent>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set this(value) {
		throw new Error("<LazyComponent>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}
}

export { LazyComponent };
//# sourceMappingURL=index.js.map
